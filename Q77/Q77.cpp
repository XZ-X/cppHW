#include<iostream>
#include"list.h"
#include"xxzString.h"
using namespace std;

list<xxzString> contentStack;
list<xxzString> symbolStack;

int main(){
	char c;
	xxzString str;
	while (cin.peek() != '\n'&&cin.peek() != EOF){
		cin >> c;
		switch (c)
		{
		default:
			break;
		}
	}

}